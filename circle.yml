#
# -*- coding: utf-8; tab-width: 4; c-basic-offset: 4; indent-tabs-mode: nil -*-

general:
  branches:
    ignore:
      - /.*-dev/

test:
  pre:
    - sudo pip install -r requirements-ci.txt
  override:
    - rst2pdf --config=./config.ini ./full-eng.rst -o $CIRCLE_ARTIFACTS/full-eng.pdf
    - rst2pdf --config=./config.ini ./quick-eng.rst -o $CIRCLE_ARTIFACTS/quick-eng.pdf

